"use client";

import { ZoneIntroScreen, type ZoneIntroScreenProps } from "../../screens/ZoneIntroScreen";

/**
 * Entrada explícita para la intro del Módulo 1.
 */
export function Module1ZoneIntroScreen(props: ZoneIntroScreenProps) {
  return <ZoneIntroScreen {...props} />;
}

export default Module1ZoneIntroScreen;
